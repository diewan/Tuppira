name: Security checks

on:
  pull_request:
  push:

jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: tuppira/scripts/secret-scan.sh
      - run: tuppira/scripts/secret-scan.sh --self-test
